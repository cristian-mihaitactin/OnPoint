@model UniSubjectsViewModel

<h2>@Model.Title</h2>
@foreach (var uniclass in Model.UniSubjects)
{
    <div>
        <h3>Titlu: @uniclass.Title</h3>
        <p>Credite: @uniclass.Credits</p>
    </div>
}